# PostgreSQL OS limits — copy to /etc/security/limits.d/99-opencnpj-postgres.conf
# Requires session restart for postgres user.

postgres soft nofile 65536
postgres hard nofile 65536
postgres soft nproc 65536
postgres hard nproc 65536

---
description: BUSCA-CNPJ-2026 — harness + Go project integration
alwaysApply: true
---

# BUSCA-CNPJ-2026 + Agent Harness

## Stack

Go 1.21+, PostgreSQL, Redis, Docker. API prefix `/api/v1/`.

## Rule sources (load order)

1. **Daily workflow:** `daily-harness-usage.mdc` — run `resolve-rules.sh` / `generate-task-rules.sh`
2. **Project:** `workflow.mdc`, `code-quality.mdc`, `testing-and-dvt.mdc`, `api-query-budget.mdc`
3. **Harness:** `agent-core-principles.mdc`, `english-only.mdc`, `context-discipline.mdc`, `token-economy.mdc`
4. **On demand:** `./agent-harness/resolve-rules.sh <keywords>`
5. **Task file (optional):** `_task-active.mdc` — remove with `generate-task-rules.sh --clean`

Rules directory: `./agent-harness/rules-path.sh` → `agent-rules/`

See `.cursor/README.md` for full command reference.

## Domain

- Glossary: `docs/GLOSSARY.md` — terms `Empresa`, `Estabelecimento`, `CNPJ`, etc.
- Architecture: `docs/ARCHITECTURE.md`
- Security: `docs/SECURITY.md` + `agent-rules/03-security/`

## Go-specific MUST

- Parameterized SQL only — no string-concat queries
- `SearchFilters` allow-list — no mass assignment
- Paginate list endpoints; respect `api-query-budget.mdc`
- Run `go test` + `go vet` before delivery (see `workflow.mdc`)

## Entry point

Read `AGENTS.md` at session start.
